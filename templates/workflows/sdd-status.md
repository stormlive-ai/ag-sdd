# @sdd-status Workflow — Spec Status Dashboard

Use this workflow to display task matrix, active tasks, and executable next tasks across all feature specs.

## Execution Steps

1. **Run CLI Commands**:
   - Run `npx ag-sdd status`
   - Run `npx ag-sdd active`
   - Run `npx ag-sdd next`
